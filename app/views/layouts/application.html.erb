<!DOCTYPE html>
<html ng-app="Sky" ng-controller="applicationCtrl as application">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "EDS" %></title>
    <meta name="description" content="<%= content_for?(:title) ? yield(:title) : "EDS" %>"/>

    <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,300,300italic,400italic,600,600italic&subset=latin,vietnamese' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'layouts/navigation' %>
    <%= render 'layouts/content' %>
    <%= yield %>
    <%= javascript_include_tag 'application' %>
    <%= yield :scripts %>
  </body>
</html>
